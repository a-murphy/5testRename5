pipelines:
  - name: basic1
    steps:
      - name: basic
        type: Bash
        configuration:
          runtime:
            type: image
        execution:
          onExecute:
            - echo {{ .Values.test }}
            
  - name: basic2
    steps:
      - name: basic
        type: Bash
        configuration:
          runtime:
            type: image
        execution:
          onExecute:
            - echo {{ .Values.test }}
            
  - name: basic3
    steps:
      - name: basic
        type: Bash
        configuration:
          runtime:
            type: image
        execution:
          onExecute:
            - echo {{ .Values.test }}
            
  - name: basic4
    steps:
      - name: basic
        type: Bash
        configuration:
          runtime:
            type: image
        execution:
          onExecute:
            - echo {{ .Values.test }}
